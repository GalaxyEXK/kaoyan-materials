---
generated_by: extract_to_md.py
source: "专业课/计算机组成原理/第四章 指令系统(下).docx"
source_sha256: "9a6fd049f5f1de90706523fb07d071a0cab123232b8a9a2b053c938cc7c52691"
extractor_profile: "mineru-vlm-v1"
---

# 第四章 指令系统(下).docx

# 第四章 指令系统(下)

## 知识点：

## 汇编指令:

x86 是一种 CPU 指令集架构 ISA (Instruction Set Architecture)，不是某一个具体 CPU。

你可以先这样记：

x86 = Intel 8086 发展出来的一系列 CPU 指令集体系。
后来的 80386、80486、Pentium、Core i5/i7/i9，以及 AMD 的很多 CPU，都属于 x86 系列或兼容 x86。

## 1. x86 到底是什么?

计算机中，CPU要执行程序，程序最后会变成一条条机器指令，例如：

这些指令的格式、寄存器名字、寻址方式、机器码编码规则，就构成了一个指令集架构 ISA。

所以：

名称

含义

x86

一套CPU指令集架构

Intel / AMD

生产x86CPU的厂商

Core i5 / Ryzen

具体CPU产品

Windows / Linux

操作系统，可以运行在x86CPU上

机器语言 / 汇编语言

直接依赖x86指令集

汇编相关缩写：https://chatgpt.com/g/g-p-6a11c4b6ac5c8191b58411f17c43bc62-ji-zu/c/6a1ffd38-c79c-83ea-b41c-3fe7c690faba

## 标志位：

## 9. 无符号比较和有符号比较的规则表

假设执行：

A - B

## 9.1 无符号比较：看 CF 和 ZF

条件

$$
\mathrm{含义}
$$

$$
Z F = 1
$$

$$
A = B
$$

$$
C F = 1
$$

$$
A <   B
$$

$$
C F = 0 \text {且} Z F = 0
$$

$$
A > B
$$

$$
C F = \theta
$$

$$
A \geq B
$$

$$
C F = 1 \text {或} Z F = 1
$$

$$
A \leq B
$$

## 无符号数比较的本质：

$$
\mathrm{CF} = 1 \rightarrow \mathrm{不够减} \rightarrow \mathrm{A} <   \mathrm{B}
$$

## 9.2 有符号比较：看 SF、OF、ZF

条件

含义

$$
Z F = 1
$$

$$
A = B
$$

$$
Z F = 0 \text {且} S F = O F
$$

$$
A > B
$$

$$
Z F = 0 \text {且} S F \neq O F
$$

$$
A <   B
$$

$$
\mathrm{SF} = 0 \mathrm{F}
$$

$$
A \geq B
$$

$$
\mathrm{SF} \neq \mathrm{OF}
$$

$$
A <   B
$$

$$
Z F = 1 \text {或} S F \neq O F
$$

$$
A \leq B
$$

## 有符号数比较的本质：

看A-B的真实符号

但如果溢出，SF 是反的

所以用 SF ⊕ OF 修正

详细解答：

https://chatgpt.com/g/g-p-6a11c4b6ac5c8191b58411f17c43bc62-ji-zu/c/6a23d7bf-77d0-83ea-8509-1aced8fb28af

https://chatgpt.com/share/6a23deef-507c-83ea-9eac-95b89b51a781

## 选择语句的机器级表示：

## #比较a和b两个数

→je <地址> #jump when equal, 若a==b则跳转

条件转移指令一般要和cmp指令一起使用

jne <地址> #jump when not equal, 若a!=b则跳转

jg <地址> #jump when greater than, 若a>b则跳转

jge <地址> #jump when greater than or equal to, 若a>=b则跳转

j1 <地址> #jump when less than, 若a<b则跳转

jle <地址> #jump when less than or equal to, 若a<=b则跳转

cmp eax, ebx #比较寄存器eax和ebx里的值

jg NEXT #若 eax > ebx，则跳转到 NEXT:




## 1. 2017年，公司与上海浦东发展银行股份有限公司签订了《关于使用部分闲置募集资金进行现金管理的协议》。

## 示例：选择语句的机器级表示

} else {




mov eax,7 #假设变量a=7,存入eax

mov ebx,6 #假设变量b=6,存入ebx

cmp eax, ebx #比较变量a和b

jg NEXT #若a>b，转移到NEXT:

mov ecx, ebx #假设用ecx存储变量c，令c=b

jmp END #无条件转移到END:

mov ecx, eax #假设用ecx存储变量c，令c=a

## 循环语句的机器级表示：

# 用条件转移指令实现循环

```c
int result = 0;
for(int i=1;i<=100;i++) {
    result +=i;
} //求 1+2+3+...+100
```

```c
int i=1;
int result = 0;
while(i<=100) {
    result +=i;
    i++;
} //求 1+2+3+...+100
```

## 三、审议通过了《关于公司符合公开发行可转换公司债券条件的议案》

for(int i=500;i>0;i--) {做某些处理；

} //循环500轮

理论上，能用 loop 指令实现的功能一定能用条件转移指令实现

使用loop指令可能会使代码更清晰简洁

补充：loopx指令——如loopnz, loopz  
loopnz——当ecx!=0 && ZF==0时，继续循环  
loopz——当ecx!=0 && ZF==1时，继续循环

## 函数调用的机器级表示：

mov eax,0 #用 eax 保存 result，初值为0

mov edx,1 #用 edx 保存 i，初始值为1

cmp edx,100 #比较 i和100

jg L2 #若i>100，转跳到L2执行

L1: #循环主体

add eax, edx #实现 result +=i

inc edx #inc 自增指令，实现 i++

cmp edx,100 #i和100

jle L1 #若 i<=100，转跳到 L1 执行

L2: #跳出循环主体

## 用loop指令实现循环

mov ecx,500

Looptop:

#用ecx作为循环计数器

#循环的开始

做某些处理

loop Looptop #ecx--, 若ecx!=0, 跳转到Looptop

等价于：dec ecx cmp ecx,0 jne Looptop push ebp




mov ebp, esp

sub esp,24

mov [ebp-12], 125

mov [ebp-8], 80

mov eax, [ebp-8]

mov [esp+4], eax

mov eax, [ebp-12]

mov esp, eax

call add

mov eax, [ebp-4]

leave

ret

## call、ret指令

push ebp

## 通常用函数名作为函数起始地址的<标号>

mov ebp, esp

mov eax, [ebp+12]

mov edx, [ebp+8]

add eax, edx

leave

## ret

函数调用指令： call<函数名>  
函数返回指令： ret

## call 指令的作用:

①将IP旧值压栈保存（保存在函数的栈帧顶部）
②设置IP新值，无条件转移至被调用函数的第一条指令
ret 指令的作用.

从函数的栈帧顶部找到IP旧值，将其出栈并恢复IP寄存器

## 注：x86处理器中

程序计数器PC（Program Counter）通常被称为IP（Instruction Pointer）

caller的栈帧

P的栈帧




main的栈帧

...其他...

## 总结：函数调用的机器级表示

## 调用者:

## 返回地址压入栈顶、并跳转到被调用函数第一条指令

执行 call 指令

一系列处理逻辑

执行ret指令 从栈顶找到返回地址，出栈并恢复IP值







## call 指令的作用:

①将IP旧值压栈保存（保存在函数的栈帧顶部）  
②设置IP新值，无条件转移至被调用函数的第一条指令

从函数的栈帧顶部找到IP旧值，将其出栈并恢复IP寄存器













总结：如何访问栈帧？

## 方法

Push // 先让esp减4，再将 🤒 压入

Pop //栈顶元素出栈写入，再让esp加4

注1: 🙏 可以是立即数、寄存器、主存地址

注2：可以是寄存器、主存地址

## 方法二:

mov 指令，结合 esp、ebp 指针访问栈帧数据

注：可以用减法/加法指令，即sub/add修改栈顶指针esp的值

ebp: 指向当前栈帧的“底部”

esp: 指向当前栈帧的“顶部”

## 在每个函数开头的“例行处理”

## 函数调用时，如何切换栈帧？

## caller:

push ebp

mov ebp, esp

sub esp,24

mov [ebp-12], 125

mov [ebp-8], 80

mov eax, [ebp-8]

mov [esp+4],eax

mov eax, [ebp-12]

mov esp,eax

call add

mov eax, [ebp-4]

leave

mov ebp, esp




mov eax, [ebp+12]

mov edx, [ebp+8]

add eax, edx

指令: enter

保存上一层函数的栈帧基址（ebp旧值）
设置当前函数的栈帧基址（ebp新值）




## 函数返回时，如何切换栈帧？

注：每个栈帧底部，用于保存上一层栈帧的基址




mov esp, ebp pop ebp

#让esp指向当前栈帧的底部
#将esp所指元素出栈，写入寄存器ebp

## 函数返回时，如何切换栈帧？




## 函数返回时，如何切换栈帧？

## IP旧值 $\rightarrow$ mov [ebp-4], eax







## 一个栈帧内可能包含哪些内容？

\- 通常将局部变量集中存储在栈帧底部区域

\- 通常将调用参数集中存储在栈帧顶部区域

## 一个栈帧内可能包含哪些内容？

上一层栈帧基址每个函数开头都要执行：push ebp  
mov ebp, esp

一定存在。用于恢复上一层函数的栈帧

若干个局部变量 局部变量。C语言中越靠前定义的局部变量越靠近栈顶

不一定存在。有些函数可能不定义局部变量

栈帧结构

未使用的区域栈帧大小为16B整数倍，因此可能产生未使用区域

不一定存在。如果其他部分刚好是16B整数倍，则不会留下“零头”

若干个调用参数调用参数。参数列表中越靠前的参数越靠近栈顶

不一定存在。有些函数调用不需要传参数

IP(返回地址)上一层栈帧基址

函数调用时，call 指令将IP寄存器值（返回地址）压栈保存一定存在（发生调用时）。但凡调用其他函数，就必须记录返回地址

## 汇编代码实战：传递返回值




IP(返回地址)上一层栈帧基址

若干个局部变量

mov [ebp-12], 125

未使用的区域

若干个调用参数

mov [esp], eax

mov [ebp-4],eax int caller(){
    int templ=125;
    int temp2=80;
    int sum=add(templ,temp2);
    return sum;
}

int add(int x, int y) {
    return x + y;

干道考研/CSKAOYAN.COM

## 总结：函数调用的机器级表示

调用者

被调用者:

保存上一层函数栈帧，设置当前函数栈帧

push ebp

可用 push 或 mov 指令实现

\- 初始化局部变量mov ebp, esp 或: enter指令

将调用参数写入当栈帧的顶部区域- 一系列处理逻辑

[ebp-4]、[ebp-8]...

返回地址压入栈顶、并跳转到被调用函数第一条指令

执行 call 指令

通过eax寄存器

使用返回值

通过eax寄存器

\- 向上层函数传递返回值

\- 恢复上一层函数的栈帧

执行ret指令mov esp, ebp pop ebp 或：leave指令

从栈顶找到返回地址出栈并恢复IP值

## 拓展：一个栈帧内可能包含哪些内容？

上一层栈帧基址

若干个局部变量

栈帧结构

未使用的区域

部分寄存器值

若干个调用参数

调用其他函数前，如果有必要，可将某些寄存器（如：eax、edx、ecx）的值入栈保存，防止中间结果被破坏不一定存在。如果这些寄存器值不是运算的中间结果，则可以不保存

IP(返回地址)

上一层栈帧基址







## CISC 和 RISC:

已思考若干秒>

微程序就是：用“更底层的一串微命令”来解释并执行一条机器指令的方法。

在计组里，尤其讲CISC指令系统时，微程序控制器非常重要。因为CISC指令复杂，一条机器指令可能要完成很多小操作，如果全部用硬布线控制会很复杂，所以常用微程序控制来实现。

## 详细讲解：

https://chatgpt.com/g/g-p-6a11c4b6ac5c8191b58411f17c43bc62-ji-zu/c/6a2514c0-2130-83ea-a80b-cc749252b038

https://chatgpt.com/c/6a251606-0538-83ea-87d1-20172682056e

## 题目：

## 题1:

11. 下列有关调用指令（转子指令）的叙述中，错误的是（）。A. 与高级语言源程序中的过程调用相对应，一次过程调用对应一条调用指令B. 指令执行时必须保留返回地址，调用指令随后一条指令的地址是返回地址C. 嵌套调用时返回地址通常保存在栈中，非嵌套调用时可保存在特定寄存器中D. 指令执行时将无条件转移到目标地址处，转移目标地址无须在指令中明显给出

11. D

为了能保证从被调用过程返回到调用过程继续执行，必须确定并保存返回地址，这个地址是调用指令随后的指令的地址，返回地址只能由调用指令来计算并保存，因为执行调用指令后就转移到了被调用过程，因此无法获取返回地址。为了保证嵌套调用时能够返回到调用过程，必须将返回地址压栈，若不压栈而保存在特定寄存器中，则后面执行的调用指令会将前面调用指令保存的返回地址覆盖掉。调用指令执行时将无条件转移到目标地址处，这个目标地址就是被调用过程第一条指令的地址，它一定在调用指令中明显给出，因此选项D错误。

https://chatgpt.com/g/g-p-6a11c4b6ac5c8191b58411f17c43bc62-ji-zu/c/6a2401c8-3a1c-83ea-a778-74cd63545203

## 题 2:

06. 程序 P 中有两个变量 i 和 j，被分别分配在寄存器 eax 和 edx 中，P 中语句 “if(i<j) {...}”对应的指令序列如下（左边为指令地址，中间为机器代码，右边为汇编指令），其中 jle 指令的偏移量为 0d:
804846a 39 c2 cmp dword ptr edx, eax
804846c 7e 0d jle xxxxxxxxx

若执行到 804846aH 处的 cmp 指令时，i = 105，j = 100，则 jle 指令执行后将转到（）处的指令执行。
A. 8048461H B. 804846eH C. 8048479H D. 804847bH

06. D

i = 105, j = 100，即 edx 的内容为 100，eax 的内容为 105，cmp 指令就是对这两个数做减法，显然 100 < 105，满足 jle 指令小于或等于的条件，jle 指令长度为 2 字节，所以 jle 指令执行后将转移到当前 PC 值 + 偏移量 = 804846cH + 2 + 0dH = 804847bH 处执行。

## 题 3:

02. 下列描述中，不符合 RISC 指令系统特点的是（）。  
A. 指令长度固定，指令种类少  
B. 寻址方式种类尽量减少，指令功能尽可能强  
C. 增加寄存器的数目，以尽量减少访存次数  
D. 选取使用频率最高的一些简单指令，以及很有用但不复杂的指令

03. 以下有关 RISC 的描述中，正确的是（）。  
A. 为了实现兼容，新设计的 RISC 是从原来 CISC 系统的指令系统中挑选一部分实现的  
B. 采用 RISC 技术后，计算机的体系结构又恢复到了早期的情况  
C. RISC 的主要目标是减少指令数，因此允许以增加每条指令的功能的方法来减少指令系统所包含的指令数  
D. 以上说法都不对

02. B

A、C 和 D 都是 RISC 的特点。对于 B，寻址方式种类尽量减少是 RISC 的特点，而增强指令的功能则是 CISC 的特点。RISC 指令功能简单，复杂指令的功能由简单指令的组合来实现。

RISC 选择一些常用的寄存器型指令，并不是为了兼容 CISC，RISC 也不可能兼容 CISC，选项 A 错误。RISC 只是 CPU 的结构发生变化，基本不影响整个计算机的结构，并且即使是采用 RISC 技术的 CPU，其架构也不可能像早期一样简单，选项 B 错误。RISC 的指令功能简单，通过简单指令的组合来实现复杂指令的功能，选项 C 错误，但 RISC 的主要目标是减少指令数是正确的。

(注：部分内容可能由 AI 生成)
